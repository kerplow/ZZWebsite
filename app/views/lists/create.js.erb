function refreshForm(innerHTML) {
    const newlistForm = $('.new_list');
    newlistForm.replaceWith(innerHTML);
};

function addlist(listHTML) {
    $('#list-form').remove()
    $('.lists').append(listHTML);
    $('#new-list-button').show()
};

<% if @list.errors.any? %>
  refreshForm('<%= j render partial: "lists/form", list: @list, container: true %>');
<% else %>
  addlist('<%= j render partial: "lists/list", list: @list, container: true %>');
<% end %>
