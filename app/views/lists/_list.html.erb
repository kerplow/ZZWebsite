<div id="list-<%= list.id %>" class="list col-sm-12 col-md-6 col-lg-4 col-xl-3">
  <h3><%= list.title %></h3>
  <ul>
    <%= render list.options unless list.private? && current_user.id.in?(list.allowed_voters) %>
  </ul>
</div>