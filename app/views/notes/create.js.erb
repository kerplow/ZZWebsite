function refreshForm(innerHTML) {
    const newNoteForm = $('.new_note');
    newNoteForm.replaceWith(innerHTML);
};

function addNote(noteHTML) {
    $('#new_note').remove()
    $('.notes').append(noteHTML);
    $('#new-note-button').show()
};

<% if @note.errors.any? %>
  refreshForm('<%= j render partial: "notes/form", note: @note %>');
<% else %>
  addNote('<%= j render partial: "notes/note", locals: {note: @note} %>');
<% end %>
