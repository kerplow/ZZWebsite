<%= javascript_include_tag "pages/home.js" %>
<div class="title text-center">
  <h1 class="text-center" id="zzw">ZZW</h1>
</div>
  <h4 class="text-center" id="site-title"><%= List.find_by(title: 'site-title').winner.name %></h4>
<% if @user %>
  <% if Rails.env.development? %>
    <%= @user.inspect %>
  <% end %>
<% else %>
  <p>not signed in yet</p>
<% end %>
<%= render partial: 'components/opruimlijst', locals: { cleaning_week: @cleaning_week } %>
<div class="row">
  <%= render partial: 'components/notitiebord' %>
  <%= render partial: 'components/planner' %>
</div>
<!-- <%#= render partial: 'pages/home/eetlijst' %> -->
<% content_for :document_ready do %>
<% end %>
